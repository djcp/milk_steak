<div class="<%= dom_class(image) %> <%= featured_image_class(image) %> thumbnail" id="<%= dom_id(image) %>">
  <% if image.image.attached? %>
    <%= link_to(image_tag(image.image_url(:thumb), class: 'w-full aspect-4/3 object-cover'), url_to_recipe(image.recipe)) %>
  <% end %>
</div>
