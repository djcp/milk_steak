<div class="max-w-md mx-auto p-6 text-center">
  <h1 class="text-6xl text-ink mb-4"><%= @status %></h1>
  <h2 class="text-2xl text-ink mb-3"><%= @title %></h2>
  <p class="text-ink-light text-base leading-relaxed mb-6"><%= @message %></p>
  <%= link_to 'Back to the cookbook', root_path, class: 'btn btn-primary' %>
</div>
<% content_for(:page_title, @title) %>